#!/bin/bash
# Source tutorial run functions
. $WM_PROJECT_DIR/bin/tools/RunFunctions;

# Extrude mesh around cylinder
(cd backgroundMesh && ./makeMesh)

# Add background mesh
(cd movingMesh && ./makeMesh)
